{
  "name": "nuxt-assets-url-bug",
  "version": "1.0.0",
  "description": "Nuxt 2.0 bug with where background: url(...) is not resolved.",
  "author": "Vyacheslav Bikbaev",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "cross-env": "^5.2.0",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
